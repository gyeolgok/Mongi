@echo off
setlocal
cd /d "%~dp0"
echo This installation keeps user data in assets/, projects/, output/, inbox/.
echo Mongi governance, canon, continuity, channel, and episode records are also preserved.
echo Future update packages should replace app/ and root batch files only.
echo Never replace the repository root with a patch archive. Apply versioned Git updates instead.
pause
